# =============================
# 基础核心配置 (复制为 .env 后修改)
# =============================
NODE_ENV=production
PORT=3000

# 数据库：填写实际云数据库连接串
DATABASE_URL=mysql://username:password@your-cloud-db-host:3306/database_name
# 示例（本地 docker mysql - 若启用 db 服务）：
# DATABASE_URL=mysql://root:your-mysql-password@db:3306/personal_game_db

# Redis（使用 compose 内 redis 服务）
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=

# JWT 密钥 (务必改成长度>=32的随机值)
JWT_SECRET=CHANGE_ME_LONG_RANDOM_STRING

# 维护模式（true|false）
MAINTENANCE_MODE=false

# 跨域允许来源（逗号分隔；留空=动态逻辑或默认）
# CORS_ALLOWED_ORIGINS=https://example.com,https://admin.example.com

# 邮件 SMTP（如使用邮件功能）
# SMTP_HOST=smtp.example.com
# SMTP_PORT=465
# SMTP_USER=your-email@example.com
# SMTP_PASS=secret
# SMTP_SECURE=true

# 速率限制（可选）
# RATE_LIMIT_WINDOW=60000   # 毫秒窗口 60s
# RATE_LIMIT_MAX=100        # 窗口内最大请求数

# 上传设置（若代码支持）
# UPLOAD_PATH=/app/backend/uploads
# MAX_FILE_SIZE=10485760

# 安全白名单/黑名单（如果中间件支持）
# IP_WHITELIST=127.0.0.1
# IP_BLACKLIST=

# =============================
# 使用说明：
# 1. 复制本文件为 .env
# 2. 修改 DATABASE_URL / JWT_SECRET 等敏感值
# 3. 生产不要提交 .env 文件到仓库
# =============================